spring.application.name=api-java

# Puerto dinámico (Render inyecta PORT, local usa 8080 por defecto)
server.port=${PORT:8080}

# Conexión a Neon (PostgreSQL) usando una sola variable
# Render: usa la variable DB_URL con usuario y contraseña embebidos
# Local: si no existe DB_URL, usa la base de pruebas por defecto
spring.datasource.url=${DB_URL:jdbc:postgresql://neondb_owner:npg_rqBEXm76kxuP@ep-little-meadow-adt6opit-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect